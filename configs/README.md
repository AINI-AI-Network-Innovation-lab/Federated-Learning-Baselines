# Config Presets

Runtime configuration lives in `pyproject.toml` under `[tool.flwr.app.config]`.

Use this folder for optional experiment notes or copied preset snippets as the
project grows. V1 intentionally keeps Flower's `pyproject.toml` as the single
source of truth.
