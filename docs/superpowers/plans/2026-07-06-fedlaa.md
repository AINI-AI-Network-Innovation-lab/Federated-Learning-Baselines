# FedLAA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add FedLAA as a new server-side layer-wise adaptive aggregation baseline in this repository.

**Architecture:** Implement a new `FedAvg`-derived strategy that reconstructs gradients from client updates, computes per-layer consensus directions, smooths client-layer angles across rounds, maps them with the paper's Gompertz function, and aggregates parameters independently per layer. Leave client-side local training on the standard path.

**Tech Stack:** Python, Flower strategies, PyTorch parameter tensors, unittest

---

### Task 1: Add failing tests for config, registry, and builder wiring

**Files:**
- Modify: `tests/test_config.py`
- Modify: `tests/test_registry.py`
- Modify: `tests/test_model_and_algorithm.py`

- [ ] Step 1: Add failing tests covering `fedlaa-beta` parsing/validation, registry inclusion, and builder fit config.
- [ ] Step 2: Run the targeted tests to verify they fail because `fedlaa` is not implemented yet.
- [ ] Step 3: Implement the minimal config and registry wiring.
- [ ] Step 4: Re-run the targeted tests to verify those cases pass.

### Task 2: Add failing strategy behavior tests

**Files:**
- Modify: `tests/test_model_and_algorithm.py`
- Create: `src/fl_baselines/algorithms/fedlaa.py`

- [ ] Step 1: Add failing tests for layer-wise aggregation, smoothed-angle tracking, and current-model builder support.
- [ ] Step 2: Run the targeted tests to verify they fail for the missing strategy.
- [ ] Step 3: Implement `FedLAAStrategy` and `FedLAABuilder` minimally to satisfy the tests.
- [ ] Step 4: Re-run the targeted tests to verify they pass.

### Task 3: Document the new baseline

**Files:**
- Modify: `README.md`
- Modify: `docs/README.md`
- Modify: `docs/algorithms/index.md`
- Modify: `docs/extending-baselines.md`
- Modify: `docs/overview.md`
- Modify: `docs/quickstart.md`
- Modify: `docs/testing-and-artifacts.md`
- Create: `docs/algorithms/fedlaa.md`

- [ ] Step 1: Update the algorithm listings and short descriptions to include FedLAA.
- [ ] Step 2: Add a dedicated `docs/algorithms/fedlaa.md` page that explains the server-side layer-wise aggregation, config, and limitations.
- [ ] Step 3: Review docs for consistency with the new config name and algorithm key.

### Task 4: Verify the integrated baseline end to end

**Files:**
- Modify: `docs/superpowers/plans/2026-07-06-fedlaa.md`

- [ ] Step 1: Run `PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s tests -v` and confirm the full suite passes.
- [ ] Step 2: Run `python -m compileall src tests` and confirm compile checks pass.
- [ ] Step 3: Remove generated `__pycache__` directories if any appear.
- [ ] Step 4: Summarize exact verification evidence and remaining limitations.
